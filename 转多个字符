#include<stdio.h>
int main()
{
	char ch;
	int count = 0;
	printf("请输入若干个字符:\n");
	while((ch = getchar()) != '\n')
	{
		if(ch >= 'A' && ch <='Z')
		{
		    printf("%c",ch + 32);
		}
		else{
			count = count + 1;
			}
	}
	printf("\n输入其他字符的个数:%d\n",count);
	return 0;
}
